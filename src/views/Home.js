import React from 'react';

import { POSTER_SIZE, BACKGROP_SIZE, IMAGE_BASE_URL } from '../helpers/config';
import noImage from '../assets/images/no_image.jpg';

const Home = () => {
    return <div>Home Page</div>
}

export default Home;